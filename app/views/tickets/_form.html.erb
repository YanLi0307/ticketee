<%= form_for [@project, @ticket] do |f| %> 
  <% if @project.errors.present? || @ticket.errors.present? %>
    <% (@project.errors.full_messages + @ticket.errors.full_messages).each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  <% end %>
  <p>
    <%= f.label :title %><br>
        </programlisting>
      </informalexample>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>
  <%= f.submit %>
<% end %>
